{
	"name": "react-suggestbar",
	"private": true,
	"description": "A simple demo for react-suggestbar package",
	"version": "1.0.0",
	"author": "jergauth",
	"dependencies": {
		"gatsby": "^2.22.15",
		"gatsby-plugin-react-helmet": "^3.3.2",
		"prop-types": "^15.7.2",
		"react": "^16.12.0",
		"react-dom": "^16.12.0",
		"react-helmet": "^6.0.0",
		"react-suggestbar": "^0.0.0"
	},
	"license": "MIT",
	"scripts": {
		"build": "gatsby build",
		"develop": "gatsby develop",
		"clean": "gatsby clean",
		"deploy": "yarn build && node ./scripts/deploy-github",
		"re": "yarn clean && rm -rf node_modules && yarn"
	},
	"devDependencies": {
		"gh-pages": "^3.0.0"
	}
}
